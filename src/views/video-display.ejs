<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Video Activity - Lesson Craft</title>
  <style>
    *{margin:0;padding:0;box-sizing:border-box}
    body{font-family:Segoe UI,Tahoma,Helvetica,Arial;background:linear-gradient(135deg,#667eea 0%,#764ba2 100%);min-height:100vh;display:flex;justify-content:center;align-items:center;padding:20px}
    .container{background:white;border-radius:16px;padding:32px;max-width:900px;width:100%;box-shadow:0 20px 60px rgba(0,0,0,.2)}
    h1{color:#667eea;margin-bottom:8px;font-size:2em;text-align:center}
    .subtitle{color:#666;margin-bottom:20px;text-align:center}
    .section{margin-bottom:24px}
    .section h2{color:#764ba2;margin-bottom:12px}
    .question-item{margin-bottom:16px;padding:12px;background:#f7f7f7;border-radius:8px}
    .question-text{font-weight:600;margin-bottom:8px}
    .options-list{list-style:none;padding-left:0}
    .options-list li{padding:8px;margin-bottom:6px;background:#fff;border-radius:6px;border:2px solid transparent}
    .options-list li.correct{background:#d4edda;border-color:#27ae60;font-weight:600}
    .top-controls{display:flex;gap:10px;margin-bottom:20px;align-items:center;}
    .top-controls button{background:linear-gradient(135deg,#667eea 0%,#764ba2 100%);color:#fff;border:none;padding:12px 22px;border-radius:12px;cursor:pointer;font-size:1.08em;font-weight:600;box-shadow:0 2px 8px rgba(102,126,234,0.10),0 1.5px 4px rgba(118,75,162,0.08);transition:background 0.2s,box-shadow 0.2s,transform 0.1s;}
    .top-controls button:hover,.top-controls button:focus{background:linear-gradient(135deg,#5a67d8 0%,#6c3483 100%);box-shadow:0 4px 16px rgba(102,126,234,0.18),0 3px 8px rgba(118,75,162,0.13);outline:none;transform:translateY(-2px) scale(1.03);}
    button.secondary{background:#fff;color:#764ba2;border:2px solid #764ba2;box-shadow:0 1.5px 4px rgba(118,75,162,0.08);}
    button.secondary:hover,button.secondary:focus{background:#f3e9fa;color:#5a67d8;border-color:#5a67d8;}
    .message{padding:10px;border-radius:6px;margin-bottom:12px;display:none}
    .message.info{display:block;background:#d1ecf1;color:#0c5460}
    .video-frame{width:100%;height:400px;border-radius:8px;margin-bottom:12px}
    .action-buttons{display:flex;gap:12px;margin-top:18px}
    .action-buttons a{flex:1;text-decoration:none}
  </style>
</head>
<body>
  <div class="container">
    <h1>🎬 Generated Video Activity</h1>
    <p class="subtitle">View and edit the generated video activity</p>

    <div id="statusMessage" class="message"></div>

    <div class="top-controls">
      <button id="editModeBtn">✏️ Edit Mode</button>
      <button id="saveChangesBtn" style="display:none;">💾 Save Changes</button>
      <button id="generatePdfBtn">📄 Generate PDF</button>
      <button id="downloadPdfBtn" style="display:none;">⬇️ Download PDF</button>
    </div>

    <div class="section">
      <h2>Video</h2>
      <div id="activityContent"></div>
    </div>

    <div class="section">
      <h2>Comprehension Questions</h2>
      <div id="questionsContent"></div>
    </div>

    <div class="section" id="openQuestionSection"></div>

    <div class="top-controls" style="margin-top:24px;">
      <a href="/video-activity"><button class="secondary">← New Video</button></a>
      <a href="/"><button class="secondary">🏠 Home</button></a>
    </div>
  </div>

  <script>
    let currentData = null;
    let currentPdfId = null;
    let isEditMode = false;

    const raw = sessionStorage.getItem('activityData');
    if (!raw) {
      window.location.href = '/video-activity';
    } else {
      try { currentData = JSON.parse(raw); } catch (e) { console.error('Invalid activityData', e); window.location.href = '/video-activity'; }
    }

    function extractJsonString(raw) {
      if (!raw) return null;
      let s = String(raw).trim();
      s = s.replace(/^```(?:json)?\s*/i, '');
      s = s.replace(/\s*```$/i, '');
      const firstBrace = s.indexOf('{');
      const lastBrace = s.lastIndexOf('}');
      if (firstBrace !== -1 && lastBrace !== -1 && lastBrace > firstBrace) {
        return s.slice(firstBrace, lastBrace + 1);
      }
      return s;
    }

    function parseGenerated(){
      if(!currentData) return {};
      const raw = currentData.generatedContent || '';
      const jsonStr = extractJsonString(raw);
      if(!jsonStr) return {};
      try{ return JSON.parse(jsonStr); } catch(e){ console.error('Failed to parse generatedContent JSON:', e, jsonStr); return {}; }
    }

    function displayActivity(){
      const container = document.getElementById('activityContent'); const parsed = parseGenerated(); let html='';
      // show video iframe if youtube url present
      const youtube = currentData.youtubeUrl || parsed.youtubeUrl || parsed.videoUrl || null;
      if (youtube) {
        // attempt to extract watch?v= id
        const m = youtube.match(/[?&]v=([A-Za-z0-9_-]{6,})/);
        const id = m ? m[1] : null;
        if (id) html += `<iframe class="video-frame" src="https://www.youtube.com/embed/${id}" frameborder="0" allowfullscreen></iframe>`;
        else html += `<div><a href="${youtube}" target="_blank">Open video source</a></div>`;
      }
      // Open question is now rendered separately
      container.innerHTML = html;
    }

    function displayQuestions(){
      const container = document.getElementById('questionsContent');
      const openQSection = document.getElementById('openQuestionSection');
      const parsed = parseGenerated();
      const multi = parsed.multiple_choice_sentences || parsed.multiple_choice_blanks || [];
      let html = '';
      multi.forEach((q, qIndex) => {
        html += `<div class="question-item" data-index="${qIndex}">`;
        const questionText = q.sentence || q.question || '';
        const editableQ = isEditMode ? 'contenteditable="true"' : '';
        html += `<div class="question-text" ${editableQ} data-field="question">${qIndex+1}. ${questionText}</div>`;
        html += '<ul class="options-list">';
        (q.options || []).forEach((opt, optIndex) => {
          const isCorrect = (typeof q.correct_option === 'number' && q.correct_option === optIndex) || (typeof q.correct_option === 'string' && q.correct_option === opt);
          const optionTextEditable = isEditMode ? 'contenteditable="true"' : '';
          html += `<li class="${isCorrect ? 'correct' : ''}" data-field="option" data-option-index="${optIndex}">`;
          html += `<span class="option-label">${String.fromCharCode(97+optIndex)})</span> `;
          html += `<span class="option-text" ${optionTextEditable}>${opt}</span>`;
          html += `<span class="option-check">${isCorrect ? ' ✓' : ''}</span>`;
          html += `</li>`;
        });
        html += '</ul></div>';
      });
      container.innerHTML = html;
      attachOptionControls();

      // Render open question at the bottom with section title
      let oqHtml = '';
      const oq = typeof parsed.open_question==='string'?parsed.open_question:parsed.open_question?.question||'';
      if (oq) {
        if (isEditMode) {
          oqHtml += `<h2>Open Question</h2><div class="question-item"><textarea id="openQuestionInput" class="question-input" style="width:100%;font-weight:600;color:#333;font-size:1.1em;margin-bottom:12px;min-height:80px;resize:vertical;padding:6px 10px;border:1px solid #ccc;border-radius:4px;background:#fff;">${oq.replace(/"/g,'&quot;')}</textarea></div>`;
        } else {
          oqHtml += `<h2>Open Question</h2><div class="question-item"><div class="question-text">${oq}</div></div>`;
        }
      }
      openQSection.innerHTML = oqHtml;
    }

    function attachOptionControls(){
      const questionItems = document.querySelectorAll('#questionsContent .question-item');
      questionItems.forEach((item, qIndex) => {
        const optionElems = item.querySelectorAll('[data-field="option"]');
        optionElems.forEach((optionElem, optIndex) => {
          const existing = optionElem.querySelector('.mark-btn');
          if (existing) existing.remove();
          if (isEditMode) {
            const btn = document.createElement('button');
            btn.type = 'button';
            btn.className = 'mark-btn';
            btn.style.cssText = 'margin-left:10px;padding:4px 8px;border-radius:6px;border:1px solid #667eea;background:#fff;color:#667eea;cursor:pointer;font-weight:600;';
            btn.textContent = optionElem.classList.contains('correct') ? 'Unmark' : 'Mark';
            btn.addEventListener('click', (e) => { e.preventDefault(); e.stopPropagation(); toggleOptionCorrectness(qIndex, optIndex); });
            optionElem.appendChild(btn);
          }
        });
      });
    }

    function toggleOptionCorrectness(questionIndex, optIndex) {
      const questionItem = document.querySelector(`#questionsContent .question-item[data-index="${questionIndex}"]`);
      if (!questionItem) return;
      const optionElems = questionItem.querySelectorAll('[data-field="option"]');
      const target = optionElems[optIndex];
      if (!target) return;
      const checkSpan = target.querySelector('.option-check');
      const already = target.classList.contains('correct');
      if (already) {
        target.classList.remove('correct');
        if (checkSpan) checkSpan.textContent = '';
      } else {
        optionElems.forEach((el, idx) => {
          const cs = el.querySelector('.option-check');
          if (idx === optIndex) {
            el.classList.add('correct');
            if (cs) cs.textContent = ' ✓';
          } else {
            el.classList.remove('correct');
            if (cs) cs.textContent = '';
          }
        });
      }
      attachOptionControls();
    }

    function toggleEditMode(){
      isEditMode=!isEditMode;
      const editModeBtn=document.getElementById('editModeBtn');
      const saveChangesBtn=document.getElementById('saveChangesBtn');
      // PDF buttons
      const generatePdfBtn=document.getElementById('generatePdfBtn');
      const downloadPdfBtn=document.getElementById('downloadPdfBtn');
      if(isEditMode){
        editModeBtn.textContent='👁️ View Mode';
        saveChangesBtn.style.display='inline-block';
        if(generatePdfBtn) generatePdfBtn.style.display='none';
        if(downloadPdfBtn) downloadPdfBtn.style.display='none';
        showMessage('Edit mode enabled. Click to edit.','info');
      } else {
        editModeBtn.textContent='✏️ Edit Mode';
        saveChangesBtn.style.display='none';
        if(generatePdfBtn) generatePdfBtn.style.display='inline-block';
        if(downloadPdfBtn && currentPdfId) downloadPdfBtn.style.display='inline-block';
        showMessage('Edit mode disabled.','info');
      }
      displayActivity(); displayQuestions();
    }

    function saveChanges(){
      try{
        const parsed = parseGenerated();
        const qItems = document.querySelectorAll('#questionsContent .question-item');
        parsed.multiple_choice_sentences = [];
        qItems.forEach((item, qIndex) => {
          const qTextEl = item.querySelector('[data-field="question"]');
          const qText = qTextEl ? qTextEl.textContent.replace(/^\d+\.\s*/,'').trim() : '';
          const opts = [];
          let correctIdx = null;
          const optionEls = item.querySelectorAll('[data-field="option"]');
          optionEls.forEach((optEl, idx) => {
            const textSpan = optEl.querySelector('.option-text');
            const txt = textSpan ? textSpan.textContent.trim() : optEl.textContent.replace(/^[a-z]\)\s*/,'').replace(/\s*✓\s*$/,'').trim();
            opts.push(txt);
            if (optEl.classList.contains('correct')) correctIdx = idx;
          });
          parsed.multiple_choice_sentences.push({ sentence: qText, options: opts, correct_option: correctIdx });
        });
        // Save open question edit
        const oqInput = document.getElementById('openQuestionInput');
        if (oqInput) {
          parsed.open_question = oqInput.value;
        }
        currentData.generatedContent = JSON.stringify(parsed);
        sessionStorage.setItem('activityData', JSON.stringify(currentData));
        sessionStorage.removeItem('currentPdfId');
        document.getElementById('downloadPdfBtn').style.display='none';
        isEditMode = false; toggleEditMode();
        showMessage('Changes saved successfully!','success');
      } catch(err){ console.error(err); showMessage('Error saving changes: '+err.message,'error'); }
    }

    function showMessage(msg,type='info'){ const el=document.getElementById('statusMessage'); el.textContent=msg; el.className='message '+type; if(type==='info'){ setTimeout(()=>{ el.className='message'; },4000); } }

    function parseGenerated(){ if(!currentData) return {}; try{ return JSON.parse(currentData.generatedContent||'{}'); }catch(e){ return {}; } }

    // PDF generation
    document.getElementById('generatePdfBtn').addEventListener('click', async ()=>{ const btn=document.getElementById('generatePdfBtn'); btn.disabled=true; btn.textContent='⏳ Generating PDF...'; showMessage('Generating PDF...','info'); try{ const response = await fetch('/api/generate-pdf', { method:'POST', headers:{ 'Content-Type':'application/json' }, body: JSON.stringify({ activityContent: currentData.generatedContent, activityId: `activity_${currentData.timestamp}`, imageTitle: currentData.imageTitle || null, customImageBase64: currentData.customImageBase64 || null }) }); if(!response.ok) throw new Error('PDF generation failed'); const result = await response.json(); if(result.success){ currentPdfId = result.pdfId; sessionStorage.setItem('currentPdfId', currentPdfId); document.getElementById('downloadPdfBtn').style.display='inline-block'; showMessage('PDF generated successfully!','success'); } else throw new Error(result.message||'Failed'); } catch(err){ console.error(err); showMessage('Error: '+err.message,'error'); } finally { btn.disabled=false; btn.textContent='📄 Generate PDF'; } });

    document.getElementById('downloadPdfBtn').addEventListener('click', ()=>{ const id = sessionStorage.getItem('currentPdfId'); if(id) window.location.href=`/api/download-pdf/${id}`; });

    document.getElementById('editModeBtn').addEventListener('click', toggleEditMode);
    document.getElementById('saveChangesBtn').addEventListener('click', saveChanges);

    // Init
    displayActivity(); displayQuestions(); const storedPdf=sessionStorage.getItem('currentPdfId'); if(storedPdf && String(currentData.timestamp)===String(sessionStorage.getItem('lastActivityTimestamp'))){ currentPdfId=storedPdf; document.getElementById('downloadPdfBtn').style.display='inline-block'; } else { sessionStorage.setItem('lastActivityTimestamp', currentData.timestamp); }
  </script>
</body>
</html>
